{
  "buildCommand": "pnpm --filter @workspace/exzibo run build",
  "outputDirectory": "artifacts/exzibo/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
